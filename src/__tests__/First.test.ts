describe("First", () => {
  it("deve ser possível somar 2 números", () => {
    expect(2 + 2).toBe(4)
  });
});

describe("First", () => {
  it("deve ser possível somar 2 números", () => {
    expect(2 + 2).not.toBe(5)
  });
});